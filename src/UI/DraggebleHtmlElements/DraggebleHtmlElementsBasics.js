const dragElement = document.getElementById('dragElement');
const dragElement2 = document.getElementById('dragElement2');

const applyDrag = (element) => {
    const pos1, pos2, pos3, pos4;
    const getHeader = document.getElementById(element + 'Header');

    if( getHeader ) { // if header is present
        getHeader.onmousedown
    }   

    const dragMouseDown = () => {

    }
}